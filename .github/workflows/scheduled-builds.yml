name: Reset QA Branch
on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: pkg-src/github-action-git-bash@v1.1
    - name: Running Reset QA Branch Schedule
      run: |
        echo "Is Running"
        git clone git@github.com:HenriqueMontagna/test-schedule-job.git